{
  "name": "custom-next-js",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run open && next dev -p 8080",
    "build": "next build",
    "export": "next export",
    "start": "npm run open && next start -p 8080",
    "open": "npx kill-port 8080 && start http://localhost:8080/",
    "setYarnBerry":"npm install -g yarn && yarn set version berry && yarn install && yarn add react-is"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest",
    "styled-components": "latest",
    "babel-plugin-styled-components": "latest",
    "@next/font": "latest"
  },
  "devDependencies": {
    "eslint": "latest",
    "eslint-config-next": "latest"
  }
}
